import { Link, useLocation } from "react-router-dom"; import "./Navbar.css"; export default function Navbar(){ const location = useLocation(); return( <nav className="navbar"> <h2>📚 BookSales</h2> <div className="nav-links"> <Link className={location.pathname==="/"?"active":""} to="/">Home</Link> <Link className={location.pathname==="/books"?"active":""} to="/books">Books</Link> <Link className={location.pathname==="/team"?"active":""} to="/team">Team</Link> <Link className={location.pathname==="/contact"?"active":""} to="/contact">Contact</Link> </div> </nav> ) }